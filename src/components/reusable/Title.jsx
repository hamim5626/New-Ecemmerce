const Title = ({title}) =>{
    return( 
        <h2 className="text-[32px] lg:text-[64px] font-prata font-normal text-heading tracking-[-2%]">
            {title}
        </h2>
    )
}

export default Title;