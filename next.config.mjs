/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'images.pexels.com', 'randomuser.me', 'mohammadnashadmin.softvencefsd.xyz'],
    },
};

export default nextConfig;
